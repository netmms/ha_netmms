# Fallback config if generation fails (should be overwritten at startup)
[stream]
stream = pipe:///share/snapserver/stream?name=Main&mode=read
sampleformat = 48000:16:2
buffer = 2000
latency = 0

[http]
enabled = true
bind_to_address = 0.0.0.0
port = 1780

[tcp]
bind_to_address = 0.0.0.0
port = 1705

[server]
stream_port = 1704
datadir = /data
